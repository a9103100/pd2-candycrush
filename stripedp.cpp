#include "stripedp.h"

stripedp::stripedp()
{

}

stripedp::~stripedp()
{

}

void stripedp::explosion(int row, int col, candy *a[10][8])
{
   /* explode *e;
    a[row][col]->type=0;
    for(int i=0;i<10;i++){
        if(a[i][col]->type/10==1){
            e=new stripedp;
            e->explosion(i,col,a[i][col]);
        }
        if(a[i][col]->type/10==2){
            e=new stripedh;
            e->explosion(i,col,a[i][col]);
        }
        if(a[i][col]->type/10==3){
            e=new wrapped;
            e->explosion(i,col,a[i][col]);
        }
        if(a[i][col]->type/10==0)
            {a[i][col]->type=0;}
    }*/
}

